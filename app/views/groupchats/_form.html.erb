<%= form_with(model: groupchat, local: true) do |form| %>
  <% if groupchat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(groupchat.errors.count, "error") %> prohibited this groupchat from being saved:</h2>

      <ul>
      <% groupchat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.text_field :group_id, id: :groupchat_group_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
